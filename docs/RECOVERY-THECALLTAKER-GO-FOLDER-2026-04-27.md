## Recovery Batch: Go-Folder Onboarding Packet

Date: 2026-04-27

This batch preserves the `docs/go-folder` onboarding and operator packet because it
contains current-truth normalization that should not stay trapped as local drift.

Dominant changes in this packet:

- normalize the live support/demo line to `+1 (629) 269-9697`
- replace stale `Jessica` references with `Gideon`
- replace stale `GHL` wording with `legacy CRM` across the old onboarding docs

This is an archival/preservation move for founder and operator collateral. It does
not claim that the old `go-folder` docs are the canonical CTOS operating system.
It keeps the packet internally consistent with the current voice identity and phone
number while broader repo cleanup continues.
